# Core runtime
PROJECT_NAME=agent-growth-advocate
ENVIRONMENT=development

# Optional API endpoints for integration tests
API_BASE_URL=http://localhost:3000
UI_BASE_URL=http://localhost:5173

# Optional test credentials (placeholder only)
TEST_USER_EMAIL=tester@example.com
TEST_USER_PASSWORD=change-me

# Optional orchestrator toggles
ENABLE_AGENT_HANDOFF_LOGS=true
ENABLE_STRICT_VALIDATION=false
