version: 1
interface:
  display_name: Prototype Intake
  short_description: Frames problem statements, goals, and constraints for downstream agents.
  default_prompt: Collect project intent, hard constraints, and success metrics before architecture starts.
